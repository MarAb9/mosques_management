services:
  app:
    build:
      context: .
    ports:
      - "127.0.0.1:${APP_PORT:-8080}:80"
    environment:
      APP_ENV: development
      APP_URL: ${APP_URL:-http://localhost}
      APP_TIMEZONE: Africa/Casablanca
      API_ACCESS_MODE: ${API_ACCESS_MODE:-public}
      API_ALLOWED_ORIGINS: ${API_ALLOWED_ORIGINS:-https://other-website.example}
      API_RATE_LIMIT: ${API_RATE_LIMIT:-120}
      API_RATE_WINDOW: ${API_RATE_WINDOW:-60}
      API_KEY_HASH: ${API_KEY_HASH:-}
      ARCGIS_ACCESS_TOKEN: ${ARCGIS_ACCESS_TOKEN:-}
      ARCGIS_ROUTING_TOKEN: ${ARCGIS_ROUTING_TOKEN:-}
      ARCGIS_STREET_TILE_URL: ${ARCGIS_STREET_TILE_URL:-https://static-map-tiles-api.arcgis.com/arcgis/rest/services/static-basemap-tiles-service/v1/arcgis/navigation/static/tile/{z}/{y}/{x}}
      ARCGIS_SATELLITE_TILE_URL: ${ARCGIS_SATELLITE_TILE_URL:-https://ibasemaps-api.arcgis.com/arcgis/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}}
      ARCGIS_SATELLITE_LABELS_URL: ${ARCGIS_SATELLITE_LABELS_URL:-https://static-map-tiles-api.arcgis.com/arcgis/rest/services/static-basemap-tiles-service/v1/arcgis/imagery/labels/static/tile/{z}/{y}/{x}}
      MAP_ROUTING_PROVIDER: ${MAP_ROUTING_PROVIDER:-arcgis}
      ARCGIS_ROUTING_URL: ${ARCGIS_ROUTING_URL:-https://route-api.arcgis.com/arcgis/rest/services/World/Route/NAServer/Route_World/solve}
      MAP_DEFAULT_LATITUDE: ${MAP_DEFAULT_LATITUDE:-34.6814}
      MAP_DEFAULT_LONGITUDE: ${MAP_DEFAULT_LONGITUDE:--1.9086}
      MAP_DEFAULT_ZOOM: ${MAP_DEFAULT_ZOOM:-9}
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-mosques_management}
      DB_USER: ${DB_USER:-mosques}
      DB_PASS: ${DB_PASS:-mosques_password}
    volumes:
      - ./public/uploads:/var/www/html/public/uploads
      - ./storage:/var/www/html/storage
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mariadb:11.4
    ports:
      - "127.0.0.1:${DB_PORT_FORWARD:-3307}:3306"
    environment:
      MARIADB_DATABASE: ${DB_NAME:-mosques_management}
      MARIADB_USER: ${DB_USER:-mosques}
      MARIADB_PASSWORD: ${DB_PASS:-mosques_password}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASS:-mosques_root_password}
    volumes:
      - db_data:/var/lib/mysql
      - ./ezyro_40059332_mosques_berkane.sql:/docker-entrypoint-initdb.d/01-mosques.sql:ro
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  phpmyadmin:
    image: phpmyadmin:5.2
    profiles: ["tools"]
    ports:
      - "127.0.0.1:${PHPMYADMIN_PORT:-8081}:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: ${DB_USER:-mosques}
      PMA_PASSWORD: ${DB_PASS:-mosques_password}
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:
